# 🚀 Как да Стартираш Приложението от Android Studio

## ⚠️ ВАЖНО: Преди да Стартираш от Android Studio

React Native приложенията **НЕ МОГАТ** да се стартират директно само от Play бутона в Android Studio, защото се нуждаят от **Metro Bundler** (JavaScript bundler) който трябва да работи преди това.

## ✅ Правилният Начин (2 стъпки)

### Стъпка 1: Стартирай Metro Bundler ПРЕДИ Android Studio

**Отвори PowerShell/Command Prompt** и изпълни:

```powershell
cd SVMessengerMobile
npm start
```

**Очаквай:** Трябва да видиш:
```
Metro waiting on exp://192.168.1.XXX:8081
```

**⚠️ НЕ ЗАТВАРЯЙ ТОЗИ ТЕРМИНАЛ!** Metro трябва да работи докато тестваш приложението.

---

### Стъпка 2: Стартирай от Android Studio

1. **Отвори Android Studio**
2. **File > Open** → Избери папката: `SVMessengerMobile/android`
   - ⚠️ НЕ отваряй целия проект (`smolyanVote`), а само `SVMessengerMobile/android` папката!
3. **Изчакай** да се синхронизира Gradle (може да отнеме 1-2 минути)
4. **Избери device/emulator** от горния dropdown (до Play бутона)
5. **Натисни Play бутона (▶️)** или **Shift+F10**

**Очаквай:** 
- Gradle ще компилира проекта
- APK ще се инсталира на emulator-а
- App ще се стартира автоматично

---

## 🔄 Алтернативен Начин (По-Бърз)

Ако искаш да стартираш всичко с една команда:

**Отвори PowerShell:**

```powershell
cd SVMessengerMobile

# Стартирай Metro в background
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd '$PWD'; npm start"

# Изчакай 5 секунди да се стартира Metro
Start-Sleep -Seconds 5

# Стартирай Android app
npm run android
```

Това ще:
1. Стартира Metro в отделен прозорец
2. Изчака да се стартира
3. Стартира Android app-а

---

## 🐛 Често Срещани Проблеми

### Проблем 1: "Unable to connect to Metro bundler"

**Причина:** Metro не е стартиран или не е достъпен

**Решение:**
1. Провери че Metro работи в терминала
2. Провери че виждаш `Metro waiting on...` съобщението
3. Ако не работи, рестартирай:
   ```powershell
   # Затвори Metro (Ctrl+C в терминала)
   # Стартирай отново
   cd SVMessengerMobile
   npm start -- --reset-cache
   ```

### Проблем 2: Android Studio не намира проекта

**Причина:** Отваряш грешната папка

**Решение:**
- ✅ Отвори: `SVMessengerMobile/android`
- ❌ НЕ отваряй: `smolyanVote` (целия проект)
- ❌ НЕ отваряй: `SVMessengerMobile` (React Native root)

### Проблем 3: "Gradle sync failed"

**Решение:**
1. **File > Invalidate Caches / Restart**
2. Избери **Invalidate and Restart**
3. Изчакай да се рестартира Android Studio
4. Опитай отново

### Проблем 4: Emulator не се стартира

**Решение:**
1. **Tools > Device Manager**
2. Стартирай emulator ръчно (Play бутон)
3. Изчакай да се зареди Android
4. Опитай отново от Android Studio

---

## 💡 Полезни Tips

1. **Hot Reload:** При промяна на код, app-ът се обновява автоматично (не трябва да рестартираш)

2. **Reload App:** Shake emulator-а (`Ctrl+M`) > "Reload"

3. **Clear Cache:** Ако имаш проблеми:
   ```powershell
   cd SVMessengerMobile
   npm start -- --reset-cache
   ```

4. **Logcat:** Виж логовете в Android Studio:
   - **View > Tool Windows > Logcat**
   - Филтрирай по `ReactNativeJS` за JavaScript логове

---

## 📋 Checklist

Преди да стартираш от Android Studio:

- [ ] Metro Bundler работи (`npm start` в терминал)
- [ ] Backend работи (`./gradlew bootRun`)
- [ ] Emulator е стартиран или физическо устройство е свързано
- [ ] Android Studio е отворен с правилната папка (`SVMessengerMobile/android`)
- [ ] Gradle синхронизация е завършена

---

## 🎯 Бърз Старт (3 команди в 3 терминала)

```powershell
# Терминал 1: Backend
./gradlew bootRun

# Терминал 2: Metro Bundler
cd SVMessengerMobile
npm start

# Терминал 3: Android App (или използвай Android Studio Play бутон)
cd SVMessengerMobile
npm run android
```

**След това:** Можеш да използваш Android Studio Play бутона за следващите стартирания (Metro трябва да работи).

---

**Последна актуализация:** 2025-12-15

