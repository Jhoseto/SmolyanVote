# 📱 Android Studio Setup - Пълно Ръководство

## 🎯 За Android Studio Emulator

Това ръководство е специално за използване на **Android Studio Emulator** за тестване на мобилното приложение с локален backend.

---

## ✅ Стъпка 1: Инсталирай Android Studio

1. Свали [Android Studio](https://developer.android.com/studio)
2. Инсталирай го
3. При първо отваряне, инсталирай Android SDK (API Level 24+)

---

## 📱 Стъпка 2: Създай Android Emulator

1. Отвори **Android Studio**
2. Кликни на **Device Manager** (иконка в дясно или Tools > Device Manager)
3. Кликни на **Create Device** (или + бутон)
4. Избери устройство (препоръчително: **Pixel 5** или **Pixel 6**)
5. Кликни **Next**
6. Избери системен образ:
   - **API Level 30+** (Android 11+) - препоръчително
   - Или по-нова версия
   - Кликни **Download** ако не е инсталиран
7. Кликни **Next** > **Finish**

---

## 🚀 Стъпка 3: Стартирай Emulator

1. В **Device Manager**, намери създадения emulator
2. Кликни на **Play бутона** (▶️) до emulator-а
3. Изчакай emulator-а да се стартира (1-2 минути при първо стартиране)

**Очаквай:** Emulator-ът трябва да се отвори и да покаже Android home screen

---

## ⚙️ Стъпка 4: Конфигурация на API URL

Мобилното приложение вече е конфигурирано за Android Emulator!

**Файл:** `SVMessengerMobile/src/config/api.ts`

```typescript
// Използва 10.0.2.2 - специален IP адрес който Android Emulator използва
// за да достъпи localhost на твоя компютър
const DEV_API_URL = 'http://10.0.2.2:2662';
const WS_URL = 'ws://10.0.2.2:2662/ws-svmessenger';
```

**Защо `10.0.2.2`?**
- Android Emulator използва специален IP адрес за да достъпи `localhost` на host машината
- `10.0.2.2` = `localhost` на твоя компютър от гледна точка на emulator-а

---

## 🔧 Стъпка 5: Стартирай Backend (Локално)

**Отвори нов терминал** и стартирай backend:

```bash
# В root директорията на проекта
./gradlew bootRun
```

**Очаквай:**
```
Started SmolyanVoteApplication in X.XXX seconds
```

**Важно:** Backend трябва да работи преди да стартираш мобилното приложение!

---

## 📦 Стъпка 6: Инсталирай Dependencies на Мобилното Приложение

**Отвори нов терминал:**

```bash
cd SVMessengerMobile
npm install
```

**Време:** ~2-5 минути

---

## 🚀 Стъпка 7: Стартирай Metro Bundler

**В същия терминал** (или нов):

```bash
cd SVMessengerMobile
npm start
```

**Очаквай:**
```
Metro waiting on exp://192.168.1.XXX:8081
```

**Не затваряй този терминал!**

---

## 📲 Стъпка 8: Стартирай Android Приложението

**Отвори нов терминал:**

```bash
cd SVMessengerMobile
npm run android
```

**Какво прави:**
1. Компилира React Native код
2. Компилира Android native код
3. Инсталира APK на emulator-а
4. Стартира приложението

**Първо стартиране:** Може да отнеме 2-5 минути

**Следващи стартирания:** По-бързо (~30 секунди)

---

## ✅ Стъпка 9: Проверка че Всичко Работи

### 1. Проверка на Emulator-а:

- ✅ Emulator-ът трябва да е отворен и работи
- ✅ Приложението трябва да се отвори автоматично

### 2. Проверка на Backend:

- ✅ Backend трябва да работи на `http://localhost:2662`
- ✅ Можеш да провериш в браузър: `http://localhost:2662/api/svmessenger/conversations` (ще изиска authentication)

### 3. Проверка на Приложението:

- ✅ Трябва да видиш **Login Screen**
- ✅ Няма crash errors

### 4. Проверка на Console Logs:

В терминала където стартира `npm start`, трябва да видиш:
- ✅ Няма red errors
- ✅ Може да видиш warnings (нормално)

---

## 🧪 Тестване

### Тест 1: Login

1. Въведи валидни credentials в приложението
2. Кликни "Login"
3. **Очаквано:**
   - ✅ Успешен login
   - ✅ Преминаване към Conversations List
   - ✅ В конзолата: "WebSocket connected"

### Тест 2: Real-time Messaging

1. Отвори conversation
2. Изпрати съобщение
3. **Очаквано:**
   - ✅ Съобщението се появява веднага
   - ✅ В конзолата: "Received message" (от WebSocket)

### Тест 3: WebSocket Оптимизация

1. Отвори приложението
2. Провери в конзолата: "WebSocket connected"
3. Натисни **Home button** на emulator-а (изпрати app в background)
4. **Очаквано:**
   - ✅ В конзолата: "App went to background, disconnecting WebSocket..."
5. Отвори app отново
6. **Очаквано:**
   - ✅ В конзолата: "App became active, connecting WebSocket..."

---

## 🐛 Често Срещани Проблеми

### Проблем 1: "Unable to connect to Metro bundler"

**Решение:**
```bash
# Затвори всички Metro процеси
# Windows:
taskkill /F /IM node.exe

# Mac/Linux:
killall node

# Стартирай отново
cd SVMessengerMobile
npm start -- --reset-cache
```

### Проблем 2: "Network request failed" или "Connection refused"

**Причина:** Backend не е достъпен или API URL е грешен

**Решение:**
1. Провери че backend работи: `http://localhost:2662`
2. Провери че `src/config/api.ts` използва `10.0.2.2:2662` (за emulator)
3. Провери че emulator-ът е стартиран

**Тест:**
```bash
# В браузър на твоя компютър, опитай:
http://localhost:2662/api/svmessenger/conversations
```

### Проблем 3: "WebSocket connection failed"

**Причина:** WebSocket URL е грешен или backend не поддържа WebSocket

**Решение:**
1. Провери `src/config/api.ts` - WS_URL трябва да е `ws://10.0.2.2:2662/ws-svmessenger`
2. Провери че backend поддържа WebSocket на `/ws-svmessenger`
3. Провери че backend е стартиран

### Проблем 4: Emulator не се стартира

**Решение:**
1. Провери че имаш достатъчно RAM (препоръчително 8GB+)
2. Провери че имаш достатъчно disk space (2GB+)
3. Опитай да рестартираш Android Studio
4. Опитай да създадеш нов emulator с по-нисък API level

### Проблем 5: App се crash-ва при стартиране

**Решение:**
1. Провери Logcat в Android Studio:
   - View > Tool Windows > Logcat
   - Филтрирай по "SVMessenger" или "ReactNative"
2. Провери че всички dependencies са инсталирани:
   ```bash
   cd SVMessengerMobile
   npm install
   ```
3. Clean build:
   ```bash
   cd SVMessengerMobile/android
   ./gradlew clean
   cd ..
   npm run android
   ```

---

## 🔍 Debugging с Android Studio

### 1. Logcat:

1. Отвори **Android Studio**
2. View > Tool Windows > **Logcat**
3. Филтрирай по:
   - `SVMessenger`
   - `ReactNative`
   - `Error`
   - `Exception`

### 2. React Native Debugger:

1. Shake emulator-а (или натисни `Ctrl+M` / `Cmd+M`)
2. Избери **"Debug"**
3. Отвори Chrome на `http://localhost:8081/debugger-ui`

### 3. Network Inspector:

1. Shake emulator-а
2. Избери **"Show Inspector"**
3. Network tab → виждаш всички API calls

---

## 📊 Проверка на Оптимизациите

### Батерия Оптимизация:

1. Отвори приложението
2. Провери в Logcat: "WebSocket connected"
3. Натисни **Home button** (изпрати app в background)
4. **Очаквано:**
   - ✅ В Logcat: "App went to background, disconnecting WebSocket..."
5. Отвори app отново
6. **Очаквано:**
   - ✅ В Logcat: "App became active, connecting WebSocket..."

### Firebase Оптимизация:

1. Отвори приложението (foreground)
2. Изпрати съобщение от друг потребител
3. **Очаквано:**
   - ✅ Няма системно notification (данните идват през WebSocket)
   - ✅ Съобщението се появява директно в app-а

---

## 📝 Checklist за Тестване

- [ ] Android Studio е инсталиран
- [ ] Emulator е създаден и стартиран
- [ ] Backend работи на `localhost:2662`
- [ ] API URL е конфигуриран (`10.0.2.2:2662`)
- [ ] Dependencies са инсталирани (`npm install`)
- [ ] Metro bundler работи (`npm start`)
- [ ] App се стартира успешно (`npm run android`)
- [ ] Login работи
- [ ] Conversations се зареждат
- [ ] Real-time messaging работи
- [ ] WebSocket оптимизация работи (затваряне в background)

---

## 🎯 Бърз Старт (3 команди)

```bash
# Терминал 1: Backend
./gradlew bootRun

# Терминал 2: Metro Bundler
cd SVMessengerMobile && npm start

# Терминал 3: Android App
cd SVMessengerMobile && npm run android
```

**Важно:** 
- Emulator трябва да е стартиран преди `npm run android`
- Backend трябва да работи преди да тестваш приложението

---

## 💡 Полезни Tips

1. **Hot Reload:** При промяна на код, app-ът се обновява автоматично (не трябва да рестартираш)

2. **Reload App:** Shake emulator-а (`Ctrl+M`) > "Reload"

3. **Clear Cache:** Ако имаш проблеми:
   ```bash
   npm start -- --reset-cache
   ```

4. **Multiple Emulators:** Можеш да стартираш няколко emulator-а и да тестваш с различни акаунти

5. **Screen Recording:** Android Studio може да записва екрана на emulator-а (Tools > Screen Record)

---

**Последна актуализация:** 2025-01-15

