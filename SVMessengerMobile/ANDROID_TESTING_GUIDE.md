# 📱 Ръководство за Стартиране и Тестване на Android Приложението

## 🎯 Предизвикателства (Prerequisites)

### 1. Инсталирани софтуери:
- ✅ **Node.js** >= 20
- ✅ **Android Studio** (с Android SDK)
- ✅ **Java JDK** 17 или по-нова версия
- ✅ **React Native CLI** (опционално, но препоръчително)

### 2. Android Studio Setup:
- Инсталирай Android SDK (API Level 24+)
- Инсталирай Android Emulator или свържи физическо устройство
- Провери че `ANDROID_HOME` environment variable е настроен

### 3. Backend трябва да работи:
- Backend трябва да е стартиран на `http://localhost:2662` (за development)
- Или промени `src/config/api.ts` за да сочи към твоя backend

---

## 🚀 Стъпка 1: Инсталиране на Dependencies

```bash
# Отиди в папката на проекта
cd SVMessengerMobile

# Инсталирай всички dependencies
npm install
```

**Време:** ~2-5 минути

---

## 🔧 Стъпка 2: Конфигурация на API

### За Development (localhost):

Ако backend-ът ти работи на `localhost:2662`, няма нужда от промени.

### За Physical Device или Emulator:

Ако използваш физическо устройство или emulator, `localhost` няма да работи!

**Решение:** Промени `src/config/api.ts`:

```typescript
// За физическо устройство или emulator, използвай IP адреса на твоя компютър
const DEV_API_URL = 'http://192.168.1.XXX:2662'; // Замени XXX с твоя IP

// За да намериш IP адреса:
// Windows: ipconfig (в Command Prompt)
// Mac/Linux: ifconfig или ip addr
```

**Пример:**
```typescript
const DEV_API_URL = 'http://192.168.1.100:2662';
const WS_URL = 'ws://192.168.1.100:2662/ws-svmessenger';
```

---

## 📱 Стъпка 3: Стартиране на Android Emulator или Свързване на Устройство

### Вариант A: Android Emulator

1. Отвори **Android Studio**
2. Кликни на **Device Manager** (Device Manager иконка)
3. Създай нов Emulator или избери съществуващ
4. Стартирай Emulator (Play бутон)

**Препоръчителни настройки:**
- API Level: 30+ (Android 11+)
- RAM: 2GB+
- Storage: 2GB+

### Вариант B: Физическо Устройство

1. Включи **Developer Options** на телефона:
   - Settings > About Phone > Tap "Build Number" 7 пъти
2. Включи **USB Debugging**:
   - Settings > Developer Options > USB Debugging
3. Свържи телефона с USB кабел
4. Провери че устройството е разпознато:
   ```bash
   adb devices
   ```
   Трябва да видиш твоето устройство в списъка

---

## 🚀 Стъпка 4: Стартиране на Metro Bundler

**Отвори нов терминал** и стартирай:

```bash
cd SVMessengerMobile
npm start
```

Или:

```bash
npm start -- --reset-cache
```

**Какво прави:** Стартира Metro bundler (JavaScript bundler за React Native)

**Очаквай:** Трябва да видиш:
```
Metro waiting on exp://192.168.1.XXX:8081
```

**Не затваряй този терминал!**

---

## 📲 Стъпка 5: Стартиране на Android Приложението

**Отвори нов терминал** (Metro bundler трябва да работи в друг терминал):

```bash
cd SVMessengerMobile
npm run android
```

**Какво прави:**
1. Компилира Android код
2. Инсталира APK на устройството/emulator-а
3. Стартира приложението

**Първо стартиране:** Може да отнеме 2-5 минути (компилира native код)

**Следващи стартирания:** По-бързо (~30 секунди)

---

## ✅ Стъпка 6: Проверка че Всичко Работи

### 1. Проверка на Приложението:

Когато приложението се отвори, трябва да видиш:
- ✅ **Login Screen** (ако не си логнат)
- ✅ Няма crash errors

### 2. Проверка на Console Logs:

В терминала където стартира `npm start`, трябва да видиш:
- ✅ Няма red errors
- ✅ Може да видиш warnings (нормално)

### 3. Проверка на Android Logcat:

За да видиш детайлни logs:

```bash
# Отвори нов терминал
adb logcat | grep -i "svmessenger\|react\|error"
```

Или в Android Studio:
- View > Tool Windows > Logcat
- Филтрирай по "SVMessenger" или "ReactNative"

---

## 🧪 Тестване на Функционалността

### Тест 1: Login

1. Въведи валидни credentials
2. Кликни "Login"
3. **Очаквано:**
   - ✅ Успешен login
   - ✅ Преминаване към Conversations List
   - ✅ В конзолата: "WebSocket connected"

### Тест 2: Conversations List

1. След login, трябва да видиш списък с conversations
2. **Очаквано:**
   - ✅ Conversations се зареждат
   - ✅ Виждаш последните съобщения
   - ✅ Виждаш unread count badges

### Тест 3: Отваряне на Chat

1. Кликни на conversation
2. **Очаквано:**
   - ✅ Отваря се Chat Screen
   - ✅ Виждаш съобщенията
   - ✅ WebSocket е активен (в конзолата)

### Тест 4: Изпращане на Съобщение

1. Въведи текст в input field
2. Кликни "Send" или натисни Enter
3. **Очаквано:**
   - ✅ Съобщението се появява веднага
   - ✅ В конзолата: "Received message" (от WebSocket)
   - ✅ Съобщението се изпраща успешно

### Тест 5: Real-time Messaging

1. Отвори приложението на два устройства/emulator-а
2. Логни се с различни акаунти
3. Изпрати съобщение от единия
4. **Очаквано:**
   - ✅ Другият получава съобщението в real-time
   - ✅ Няма нужда от refresh

### Тест 6: Push Notifications (Firebase)

1. Затвори приложението (или го изпрати в background)
2. Изпрати съобщение от друг потребител
3. **Очаквано:**
   - ✅ Получаваш push notification
   - ✅ При кликване → приложението се отваря
   - ✅ Виждаш новото съобщение

### Тест 7: WebSocket Оптимизация (Батерия)

1. Отвори приложението
2. Провери в конзолата: "WebSocket connected"
3. Изпрати app в background (Home button)
4. **Очаквано:**
   - ✅ В конзолата: "App went to background, disconnecting WebSocket..."
5. Върни app в foreground
6. **Очаквано:**
   - ✅ В конзолата: "App became active, connecting WebSocket..."
   - ✅ WebSocket се свързва отново

---

## 🐛 Често Срещани Проблеми

### Проблем 1: "Unable to connect to Metro bundler"

**Решение:**
```bash
# Затвори всички Metro процеси
# Windows:
taskkill /F /IM node.exe

# Mac/Linux:
killall node

# Стартирай отново
npm start -- --reset-cache
```

### Проблем 2: "Network request failed" или "Connection refused"

**Причина:** Backend не е достъпен или API URL е грешен

**Решение:**
1. Провери че backend работи: `http://localhost:2662`
2. Ако използваш физическо устройство, промени `src/config/api.ts` с IP адреса
3. Провери че firewall не блокира порта

### Проблем 3: "WebSocket connection failed"

**Причина:** WebSocket URL е грешен или backend не поддържа WebSocket

**Решение:**
1. Провери `src/config/api.ts` - WS_URL трябва да е правилен
2. Провери че backend поддържа WebSocket на `/ws-svmessenger`
3. За физическо устройство, използвай `ws://IP:PORT/ws-svmessenger`

### Проблем 4: "Firebase not initialized"

**Причина:** `google-services.json` липсва или е неправилен

**Решение:**
1. Провери че `android/app/google-services.json` съществува
2. Провери че файлът е правилно форматиран JSON
3. Rebuild проекта:
   ```bash
   cd android
   ./gradlew clean
   cd ..
   npm run android
   ```

### Проблем 5: App се crash-ва при стартиране

**Решение:**
1. Провери Logcat за errors:
   ```bash
   adb logcat | grep -i "error\|exception\|crash"
   ```
2. Провери че всички dependencies са инсталирани:
   ```bash
   npm install
   ```
3. Clean build:
   ```bash
   cd android
   ./gradlew clean
   cd ..
   npm run android
   ```

---

## 📊 Проверка на Оптимизациите

### Батерия Оптимизация:

1. Отвори приложението
2. Остави го в background за 10 минути
3. Провери в Settings > Battery:
   - ✅ App не трябва да консумира много батерия
   - ✅ WebSocket трябва да е затворен в background

### Firebase Оптимизация:

1. Отвори приложението (foreground)
2. Изпрати съобщение от друг потребител
3. **Очаквано:**
   - ✅ Няма системно notification (данните идват през WebSocket)
   - ✅ Съобщението се появява директно в app-а

### Debounce Оптимизация:

1. Получи няколко notifications бързо един след друг
2. **Очаквано:**
   - ✅ Conversations refresh само веднъж (след 500ms debounce)
   - ✅ Не се правят множество API calls

---

## 🔍 Debugging Tips

### 1. React Native Debugger:

Инсталирай [React Native Debugger](https://github.com/jhen0409/react-native-debugger)

```bash
# Windows (Chocolatey):
choco install react-native-debugger

# Mac (Homebrew):
brew install --cask react-native-debugger
```

### 2. Chrome DevTools:

1. Shake устройството (или `adb shell input keyevent 82`)
2. Избери "Debug"
3. Отвори Chrome на `http://localhost:8081/debugger-ui`

### 3. Flipper (Опционално):

Инсталирай [Flipper](https://fbflipper.com/) за по-добро debugging

---

## 📝 Checklist за Тестване

- [ ] App се стартира без errors
- [ ] Login работи
- [ ] Conversations се зареждат
- [ ] Chat screen работи
- [ ] Изпращане на съобщения работи
- [ ] Real-time messaging работи (WebSocket)
- [ ] Push notifications работят (Firebase)
- [ ] WebSocket се затваря в background
- [ ] WebSocket се отваря при foreground
- [ ] Debounce работи (няма излишни API calls)
- [ ] Батерията не се хаби много

---

## 🎯 Следващи Стъпки

След като тестваш всичко:

1. **Тествай на различни устройства** (различни Android версии)
2. **Тествай performance** (голям брой съобщения)
3. **Тествай offline scenarios** (без интернет)
4. **Тествай push notifications** (когато app е closed)

---

**Последна актуализация:** 2025-01-15

