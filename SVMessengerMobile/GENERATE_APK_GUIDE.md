# 📱 Ръководство за Генериране на Release APK

## 🎯 Цел
Генериране на production APK файл за инсталация на телефон, който да работи с production backend (`https://smolyanvote.com`).

## ⚠️ Проблем с Дългите Пътища на Windows
React Native 0.83 с New Architecture изисква компилиране на native модули, което може да причини проблеми с дългите пътища на Windows (>260 символа). Затова препоръчваме използването на Android Studio.

---

## 🚀 Метод 1: Използване на Android Studio (Препоръчително)

### Стъпка 1: Отворете проекта в Android Studio
1. Отворете **Android Studio**
2. **File > Open**
3. Изберете папката: `SVMessengerMobile/android`
4. Изчакайте Gradle sync да завърши

### Стъпка 2: Bundle на JavaScript кода
Преди да генерирате APK, трябва да bundle-нете JavaScript кода за production:

```powershell
cd SVMessengerMobile
npx react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res/
```

### Стъпка 3: Генериране на APK
1. В Android Studio: **Build > Generate Signed Bundle / APK**
2. Изберете **APK** (не Android App Bundle)
3. Натиснете **Next**

### Стъпка 4: Избор на Keystore
За тестване можете да използвате debug keystore:
- **Key store path**: `SVMessengerMobile/android/app/debug.keystore`
- **Key store password**: `android`
- **Key alias**: `androiddebugkey`
- **Key password**: `android`

Натиснете **Next**

### Стъпка 5: Избор на Build Variant
- Изберете **release** build variant
- Натиснете **Finish**

### Стъпка 6: Намиране на APK файла
APK файлът ще бъде генериран в:
```
SVMessengerMobile/android/app/release/app-release.apk
```

---

## 🔧 Метод 2: Използване на Gradle (Алтернатива)

Ако искате да опитате с Gradle команда:

```powershell
cd SVMessengerMobile

# Първо bundle на JavaScript
npx react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res/

# След това генериране на APK
cd android
.\gradlew.bat assembleRelease
```

**Забележка**: Ако получите грешка за дълги пътища, използвайте Метод 1 (Android Studio).

---

## 📱 Инсталация на Телефона

### Стъпка 1: Копиране на APK
Копирайте `app-release.apk` файла на вашия телефон (чрез USB, email, или cloud storage).

### Стъпка 2: Разрешаване на инсталация от неизвестни източници
1. На телефона: **Settings > Security**
2. Разрешете **Install from unknown sources** или **Install unknown apps**
3. Изберете приложението/браузъра, който ще използвате за инсталация

### Стъпка 3: Инсталация
1. Отворете APK файла на телефона
2. Натиснете **Install**
3. След като инсталацията завърши, натиснете **Open**

---

## ✅ Проверка на Production Конфигурация

Приложението автоматично използва production backend когато `__DEV__ = false` (което е така в release build):

- **API URL**: `https://smolyanvote.com`
- **WebSocket URL**: `wss://smolyanvote.com/ws-svmessenger-ws`

Това означава, че приложението ще се свързва директно с production сървъра.

---

## 🐛 Често Срещани Проблеми

### Проблем 1: "Filename longer than 260 characters"
**Решение**: Използвайте Android Studio (Метод 1) вместо Gradle команда.

### Проблем 2: "Unable to install APK"
**Решение**: 
- Проверете че сте разрешили инсталация от неизвестни източници
- Проверете че APK файлът не е корумпиран (опитайте да го генерирате отново)

### Проблем 3: "App crashes on startup"
**Решение**:
- Проверете че backend сървърът работи на `https://smolyanvote.com`
- Проверете че имате интернет връзка
- Проверете логите в Android Studio Logcat

---

## 📝 Забележки

- ⚠️ Debug keystore се използва само за тестване. За production трябва да генерирате собствен keystore.
- ⚠️ APK файлът е подписан с debug ключ, така че не може да бъде публикуван в Google Play Store.
- ✅ За тестване на телефона с production backend, debug keystore е напълно достатъчен.

---

## 🎉 Готово!

След като инсталирате APK файла на телефона, приложението ще работи с production backend на `https://smolyanvote.com`.

