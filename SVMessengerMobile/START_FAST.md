# 🚀 БЪРЗО СТАРТИРАНЕ - SVMessenger Mobile

## ⚡ ЕДНА КОМАНДА ЗА ВСИЧКО

```powershell
cd SVMessengerMobile
npm run quick-start
```

**Това е!** Скриптът автоматично:
- ✅ Проверява дали Metro работи
- ✅ Стартира Metro ако не работи (в отделен прозорец)
- ✅ Проверява за Android устройство
- ✅ Стартира приложението БЪРЗО

---

## 📊 ОПТИМИЗАЦИИ

### 1. Gradle Оптимизации
- ✅ **Паралелно компилиране** (`org.gradle.parallel=true`)
- ✅ **Build кеширане** (`org.gradle.caching=true`)
- ✅ **On-demand конфигурация** (`org.gradle.configureondemand=true`)
- ✅ **Повече памет** (4GB вместо 2GB)
- ✅ **Gradle daemon** винаги активен

### 2. Metro Оптимизации
- ✅ **Inline requires** за по-бързо стартиране
- ✅ **Оптимизиран asset handling**
- ✅ **По-малко overhead** от watchers

### 3. Скрипт Оптимизации
- ✅ **Без излишни забавяния** (3 секунди вместо 10)
- ✅ **Автоматична проверка** на Metro
- ✅ **Минимизиран прозорец** за Metro
- ✅ **Бърза проверка** на устройства

---

## 🎯 АЛТЕРНАТИВНИ МЕТОДИ

### Метод 1: Ако Metro вече работи
```powershell
cd SVMessengerMobile
npm run android:fast
```

### Метод 2: Ръчно стартиране
```powershell
# Терминал 1: Metro
cd SVMessengerMobile
npm start:fast

# Терминал 2: Android
cd SVMessengerMobile
npm run android
```

---

## ⚡ ОЧАКВАНО ВРЕМЕ

- **Първо стартиране**: 30-60 секунди (компилиране)
- **Следващи стартирания**: 10-20 секунди (кеш)
- **Hot reload**: Мгновено (< 1 секунда)

---

## 🐛 АКО ИМА ПРОБЛЕМИ

### Проблем 1: Metro не стартира
```powershell
cd SVMessengerMobile
npm run stop-metro
npm start
```

### Проблем 2: Gradle бавно компилира
```powershell
cd SVMessengerMobile/android
.\gradlew.bat clean
.\gradlew.bat assembleDebug
```

### Проблем 3: Кеш проблеми
```powershell
cd SVMessengerMobile
npm start -- --reset-cache
```

---

## 💡 TIPS

1. **Първо стартиране**: Оставете да завърши (компилира всичко)
2. **Следващи стартирания**: Ще са МНОГО по-бързи (кеш)
3. **Hot Reload**: Работи автоматично при промяна на код
4. **Metro прозорец**: Може да го минимизирате, не го затваряйте

---

## ✅ CHECKLIST

Преди да стартираш:
- [ ] Backend работи (`./gradlew bootRun`)
- [ ] Android устройство/емулатор е готов
- [ ] Node.js >= 20 е инсталиран

---

**🎉 Готово! Сега стартирането е МНОГО по-бързо!**

